## 1. Implementation

- [x] 1.1 Add `"when": "editorFocus"` to both keybinding entries in `package.json`

## 2. Tests

- [ ] 2.1 Add test cases verifying the shortcut `when` clause is present in `package.json` for both commands

## 3. Documentation

- [ ] 3.1 Update the related section in README.md
