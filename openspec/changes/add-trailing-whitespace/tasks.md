## 1. Core Implementation

- [ ] 1.1 Modify `copyPath` in `src/extension.ts` to append trailing space (single-line) or trailing newline (multi-line) to the clipboard text

## 2. Tests

- [ ] 2.1 Update existing tests to expect trailing whitespace in clipboard output
